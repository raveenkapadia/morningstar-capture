{
  "name": "morningstar-backend",
  "version": "1.0.0",
  "description": "MorningStar.ai — Preview generation + pipeline backend",
  "main": "index.js",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod",
    "generate": "node scripts/generate.js",
    "test:capture": "node scripts/test-capture.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.39.0",
    "node-fetch": "^3.3.2"
  },
  "devDependencies": {
    "vercel": "^33.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
